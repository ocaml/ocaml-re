(env
 (dev
  (flags
   (:standard -w -58))))

(executables
 (enabled_if
  (not %{env:CI=false}))
 (libraries
  re
  core
  base
  stdio
  threads
  core_bench
  core_unix.command_unix
  memtrace)
 (modules :standard \ compare)
 (names benchmark))

(rule
 (alias runtest)
 (enabled_if
  (not %{env:CI=false}))
 (deps files http-requests.txt tex.gitignore)
 (action
  (chdir
   %{workspace_root}
   (setenv
    RE_BENCH_FILTER
    http
    (ignore-stdout
     (run %{exe:benchmark.exe} -thin-overhead 1))))))

(executable
 (enabled_if
  (not %{env:CI=false}))
 (name compare)
 (modules compare)
 (libraries
  core
  csv
  base
  core_unix
  core_unix.command_unix
  core_unix.filename_unix
  spawn
  stdio
  sexplib))
